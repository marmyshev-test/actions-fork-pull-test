# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on: [push, pull_request, pull_request_target]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        env:
          S1: ${{ secrets.MY_SECRET }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
          echo secret S1: ${S1}
          echo secret GITHUB_TOKEN: ${GITHUB_TOKEN}
          if [ -z "${S1}" ]; then echo "secret var is Unset!"; else echo "secret var is set"; fi
          echo README:
          cat README.md
      
      - name: Optional push
        if: github.event_name == 'push'
        run: echo Push optional
        
      - name: Optional pull_request
        if: github.event_name == 'pull_request'
        run: echo pull_request optional
      
      - name: Optional pull_request_target
        if: github.event_name == 'pull_request_target'
        run: echo pull_request_target optional
